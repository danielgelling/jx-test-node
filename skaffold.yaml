apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/danielgelling/jx-test-node:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/danielgelling/jx-test-node:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: jx-test-node
        chartPath: charts/jx-test-node
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/danielgelling/jx-test-node"
          image.tag: "{{.TAG}}"
